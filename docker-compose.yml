services:
  api:
    build: .
    container_name: mini-erp-api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-5000}:5000"

  # local mongodb then set DATABASE_URL=mongodb://mongo:27017/mini-erp
  # mongo:
  #   image: mongo:7
  #   container_name: mini-erp-mongo
  #   restart: unless-stopped
  #   volumes:
  #     - mongo_data:/data/db
  #   ports:
  #     - "27017:27017"

# volumes:
#   mongo_data:
